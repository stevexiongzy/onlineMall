description '${projectName}-appserver'

dependencies {
    compile project(':${projectName}-api')
    compile project(':${projectName}-infrastructure')
    compile project(':${projectName}-domain')
}
