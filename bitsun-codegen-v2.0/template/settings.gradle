rootProject.name = '${projectName}'

include ':${projectName}-server'
include ':${projectName}-facade'
include '${projectName}-dto'
include ':${projectName}-facade:${projectName}-api'
include ':${projectName}-facade:${projectName}-provider'